{
  "name": "@shermant/i18n-translator",
  "private": true,
  "type": "module",
  "version": "1.3.1",
  "description": "A CLI tool to help you extract and translate i18n text",
  "author": "ShermanT",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:all": "bun install",
    "clean": "bun run -p clean",
    "build": "bun run -p build",
    "dev": "bun run -p dev",
    "test": "bun run -p test",
    "lint": "eslint .",
    "format": "eslint . --fix"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^2.20.0",
    "eslint": "^9.4.0",
    "eslint-plugin-format": "^0.1.1"
  }
}
